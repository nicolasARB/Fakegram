export default function Post(props) {
    {/*just waits for the post info */}
    return<div className="min-w-full h-3/5">
        <img src={props.postUrl} alt={props.postAlt} className="w-full h-auto" />
    </div>
};
